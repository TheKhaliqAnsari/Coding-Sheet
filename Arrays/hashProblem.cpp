#include<bits/stdc++.h>
using namespace std;

int main(){

    vector<int> v = {2,1,2,4,1,2,3,6,4,8,2};
    map<int, int> m;
    map<int, int> n;

    for(int i = 0; i < v.size(); i++){
        if(m.count(v[i]))
    }
    for(int i = v.size() - 1; i >= 0; i--){
        n[v[i]] = i;
    }
    
    
    }

    return 0;
}